// service-worker.js
self.addEventListener('install', event => {
    // Cache assets
});

self.addEventListener('fetch', event => {
    // Responde con un objeto de la caché o realiza una solicitud de red
});
